import {SiteHeader} from '@/components/site-header';import {RentalBox} from '@/components/rental-box';import {getGearById} from '@/lib/data';
export default async function Detail({params}:{params:Promise<{id:string}>}){const gear=await getGearById((await params).id);return <><SiteHeader/><main className="shell detail"><div className="detail-img" style={{backgroundImage:`url(${gear.images?.[0]})`}}></div><div><div className="eyebrow" style={{color:'#1d5a45'}}>{gear.category?.name||'Outdoor gear'} · {gear.brand}</div><h1>{gear.name}</h1><div className="price">${gear.pricePerDay} <small>/ day</small></div><p className="muted" style={{lineHeight:1.6}}>{gear.description}</p><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}>{Object.entries(gear.specifications||{}).map(([k,v])=><div key={k} style={{padding:'12px 0',borderTop:'1px solid #dfe5df'}}><small className="muted">{k}</small><br/><b>{v}</b></div>)}</div><RentalBox gearId={gear.id}/><p className="muted">Provided by <b>{gear.provider?.fullName||'GearUp provider'}</b></p></div></main></>}
