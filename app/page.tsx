import Link from 'next/link';import {SiteHeader} from '@/components/site-header';import {GearCard} from '@/components/gear-card';import {getGear} from '@/lib/data';
export default async function Home(){const gear=await getGear();return <><SiteHeader/><main className="shell"><section className="hero"><div className="eyebrow">Rental gear, zero hassle</div><h1>Get outside.<br/>We’ll handle<br/>the gear.</h1><p>From first tracks to backcountry nights, rent premium equipment for whatever you’re chasing next.</p><Link href="/gear" className="btn lime">Explore the collection →</Link></section><section className="section"><div className="section-head"><div><div className="eyebrow" style={{color:'#1d5a45'}}>Choose your playground</div><h2>Gear for every kind of day.</h2></div><Link className="muted" href="/gear">View all gear →</Link></div><div className="category-grid">{[['⛺','Camping'],['🏂','Snow sports'],['🚲','Cycling'],['🛶','Water sports']].map(([icon,name])=><Link href="/gear" className="category" key={name}><span>{icon}</span>{name} <small className="muted">Explore →</small></Link>)}</div></section><section className="section"><div className="section-head"><div><div className="eyebrow" style={{color:'#1d5a45'}}>Top picks</div><h2>Ready when you are.</h2></div><Link className="muted" href="/gear">Browse all →</Link></div><div className="gear-grid">{gear.slice(0,4).map(item=><GearCard key={item.id} gear={item}/>)}</div></section><section className="section" id="why"><div className="band"><div><div className="eyebrow">Made for more time outside</div><h2>Own the experience, not the gear.</h2><p style={{color:'#c6d2cb'}}>Book with confidence. Flexible dates, verified providers, and equipment that’s ready to go.</p></div><Link className="btn lime" href="/auth/register">Join GearUp →</Link></div></section></main></>}
